// This file is now deprecated - use config/index.ts instead
// Keeping for backward compatibility
import { config } from "@/config";

export const portfolio = config.portfolio.items;

// Recent mall installations data
export const recentInstalls = config.recentWork.items;
